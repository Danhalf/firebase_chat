import {deepOrange} from "@mui/material/colors";

interface IStyles {
    navbarHeight: number,
    secondaryColor: string,
}

export const styles: IStyles = {
    navbarHeight: 80,
    secondaryColor: deepOrange["500"]
}